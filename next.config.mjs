/**
 * Next.js config for a minimal scaffold.
 * - No images or proprietary assets are included.
 */
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // App Router structure
    typedRoutes: true
  }
};

export default nextConfig;

